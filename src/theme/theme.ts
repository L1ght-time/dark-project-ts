import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },
    // typography: {},
    // spacing: {},
    // breakpoints,
    // zIndex,
    // transitions,
    // components,
  },
});

export default theme;
