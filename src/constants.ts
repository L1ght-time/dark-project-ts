export enum AuthRoutes {
  RoutePathDashboard = '/',
  RoutePathProfile = 'profile',
}

export enum GuestRoutes {
  RouteAuthorization = 'sign-in',
  RouteRegistration = 'sign-up',
  RouteForgotPassword = 'forgot-password',
  RouteNotFound = 'not-found',
}
